{
  "name": "qa-app",
  "version": "1.0.0",
  "description": "Simple Node + Express Q&A app with cloud MongoDB storage (falls back to local JSON files for local dev)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.2",
    "mongodb": "^7.6.0"
  }
}
